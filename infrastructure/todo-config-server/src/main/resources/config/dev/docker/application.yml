logging:
  level:
    io.agatsenko.todo: INFO

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    serviceUrl:
      defaultZone: http://todo-eureka-server:9101/eureka/
